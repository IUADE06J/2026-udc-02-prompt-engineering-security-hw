# Keep the agent's context focused: hide noise, builds, deps, and secrets.
# Task D - context layer (+ security: keep .env out of agent index).

# Dependencies
node_modules/
app/node_modules/

# Env / secrets
.env
.env.*
app/.env
app/.env.*

# Builds
dist/
app/dist/
build/
app/coverage/
coverage/
app/.next/
app/out/

# Lockfiles
package-lock.json
pnpm-lock.yaml
yarn.lock
app/package-lock.json

# Logs
*.log
npm-debug.log*

# OS / editor
.DS_Store
nul
